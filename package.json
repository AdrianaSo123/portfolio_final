{
  "name": "portfolio-final-workspace",
  "private": true,
  "workspaces": [
    "site"
  ],
  "scripts": {
    "postinstall": "npm --prefix site install",
    "dev": "npm --prefix site run dev",
    "build": "npm --prefix site run build",
    "start": "npm --prefix site run start",
    "lint": "npm --prefix site run lint",
    "a11y:check": "npm --prefix site run a11y:check"
  }
}
